export type TbuttonProps = {
    toggleMenu: () => void,
    isActive: boolean;
};

export type TnavProps = {
    toggleMenu: () => void,
};

export type TperspectiveTextProps = {
    label: string;
};